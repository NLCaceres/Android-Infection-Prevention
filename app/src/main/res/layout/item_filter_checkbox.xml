<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="50dp">

<!-- Used in place of imageview plus textview -->
  <androidx.appcompat.widget.AppCompatCheckedTextView
      android:id="@+id/filterCheckNameTextView"
      android:layout_width="0dp"
      android:layout_height="0dp"
      android:paddingStart="20dp"
      android:paddingEnd="20dp"
      android:paddingTop="10dp"
      android:paddingBottom="10dp"
      android:text="@string/default_filter_name"
      android:textSize="20sp"
      android:checkMark="@drawable/custom_checkbox"
      android:gravity="center_vertical"
      app:layout_constraintStart_toStartOf="parent"
      app:layout_constraintEnd_toEndOf="parent"
      app:layout_constraintTop_toTopOf="parent"
      app:layout_constraintBottom_toBottomOf="parent" />
  <!-- Checkmark seems to REQUIRE vectors in the selector for some reason. layer-lists, shapes etc don't seem allowed -->

</androidx.constraintlayout.widget.ConstraintLayout>